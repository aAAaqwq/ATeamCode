# Prototypes

Runtime experiments are isolated here. Prototype structure and configuration
must not become canonical control-plane contracts by accident.

- `codex-app-server/` — candidate primary execution backend
- `deepseek-harness/` — pinned experimental bundle/profile backend
